'use strict'

const gLevel = {
    HEIGHT: 9,
    WIDTH: 11,
    MINES: 10
}

function setLevel(height, width, mines) {
    gLevel.HEIGHT = height
    gLevel.WIDTH = width
    gLevel.MINES = mines
}

